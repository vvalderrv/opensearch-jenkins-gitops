---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: opensearch-1password-sa-token-staging
  namespace: jenkins-staging
spec:
  secretStoreRef:
    kind: SecretStore
    name: onepassword-releng
  target:
    creationPolicy: Owner
  data:
    - secretKey: credential
      remoteRef:
        key: Opensearch 1Password svc-opensearch-staging/credential
